# Copy this file to .env.local and adjust as needed.

# Used only by `npm run generate:qr` to build the QR code's target URL
# (https://<this>/menu). Change this to the real production domain once
# it's registered, then re-run `npm run generate:qr`.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
